//conditional statement - 
// if else statements 
// if else 9if statement
// switch statements This will handle multiple conditions based on expressions
// Ternry operator (?:)- This is a short hand of if else 
  
    let age = 12

    if(age >=20){
        console.log("Age is greater than 20");
    }
    //cross browwser -- same tests are executed on different browsers like chrome , edge etc 
    // cross platform ---- to execute same set of tests on difff OS like mac, windows, linux


    //Ternary operatior - 
    //(condition)? (condition becomes true): (condition becomes false)
// There should be a mandatory ; ending statement before the Ternary statement
    (age >18)?console.log("Eleigible for voting"):console.log("Not eligible for voting")